[package]
name = "waft-desktop"
version = "0.1.0"
description = "Desktop companion for waft file sharing"
authors = ["waft contributors"]
edition = "2024"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
